# Лабораторная работа №5. Сравнение функций. Мемоизация
Работал: Шлендов М.А. 2-й курс ИВТ-2.
## Задание:
Написать функцию get_currencies(currency_codes, url), которая обращается к API по url (по умолчанию - https://www.cbr-xml-daily.ru/daily_json.js) и возвращает словарь курсов валют для валют из списка currency_codes.

В возвращаемом словаре ключи - символьные коды валют, а значения - их курсы.

В случае ошибки запроса функция должна вернуть None.

Для обращения к API использовать функцию get модуля requests.

Для установки requests можно использовать команду:

pip install requests.

### Итерация 1
Предусмотреть в функции логирование ошибок с использованием стандартного потока вывода (sys.stdout).

Функция должна обрабатывать следующие исключения:

в ответе не содержатся курсы валют;
в словаре курсов валют нет валюты из списка currency_codes;
ошибка выполнения запроса к API.

### Итерация 2.
Вынести логирование ошибок из функции get_currencies(currency_codes, url) в декоратор.

### Итерация 3.
Оформить логирование ошибок с использованием модуля logging.

Документация по logging: https://docs.python.org/3/library/logging.html

Туториал по logging: https://docs.python.org/3/howto/logging.html

### Тестирование функций должно содержать:
проверку ключей и значений возвращаемого словаря;
проверку обработки исключений;
проверку записей логов в поток вывода.


## Объяснение решения:
### Итерация 1: Базовая функция с обработкой исключений
Функция get_currencies обрабатывает основные исключения:

requests.RequestException - ошибки запроса к API

KeyError - отсутствие ожидаемых данных в ответе

ValueError - отсутствие запрошенных валют в ответе

Логирование через sys.stdout с использованием print

### Итерация 2: Вынесение логирования в декоратор
Создан декоратор @log_errors, который перехватывает исключения

Основная функция стала чище и сосредоточена на бизнес-логике

### Итерация 3: Использование модуля logging
Добавлено логирование через стандартный модуль logging

Настроен формат вывода и уровень логирования

Сохранена обратная совместимость с выводом в sys.stdout

Особенности реализации:
Типизация для лучшей читаемости кода

Проверка входных данных

Детальные сообщения об ошибках

Соответствие всем требованиям задания

Функция корректно обрабатывает все указанные сценарии ошибок и возвращает None в случае любых проблем с получением данных.

